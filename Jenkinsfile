
pipeline {
    agent any
    stages {
        stage('prep') {
            steps {
                sh 'echo Step1 in Prep'
            }
        }
        stage('build') {
            steps {
                sh 'echo Step1 in Build'
            }
        }
    }
}
